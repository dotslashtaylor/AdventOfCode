package adventofcode2020.{{ day }};

import adventofcode2020.Solution;

import java.util.ArrayList;
import java.util.Scanner;
import java.io.File;

public class {{ ClassName }} extends Solution<SolutionObject, DataObject> {

	public {{ ClassName }}() {
		inputRelPath = "./resources/inputs/day{{ number }}.txt";
		identifier = "Day {{ day }}: {{ ClassName }}";
	}

	public ArrayList<DataObject> getData() {
		ArrayList dataList = new ArrayList();
		try {
			File dataFile = new File(inputRelPath);
			Scanner scan = new Scanner(dataFile);
			while (scan.hasNextLine()) {
				dataList.add(scan.nextLine());
			}
		}
		catch(Exception exception) {
			System.err.println(exception);
			System.exit(1);
		}
		return dataList;
	}

	public SolutionObject partOne(ArrayList<DataObject> dataList) {
		int solution = 0;

		return solution;
	}

	public SolutionObject partTwo(ArrayList<DataObject> dataList) {
		int solution = 0;

		return solution;
	}

}
